require 'spec_helper'

describe Rspec::JsonSchema do
  it 'has a version number' do
    expect(Rspec::JsonSchema::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(false)
  end
end
